{
  "name": "take_home_project",
  "version": "1.0.0",
  "description": "지원자 : 박준호",
  "main": "index.js",
  "scripts": {
    "test": "npx playwright test",
    "test:mock": "cross-env MOCK=true npx playwright test",
    "test:local": "cross-env MOCK=false npx playwright test"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@playwright/test": "^1.54.2",
    "@stoplight/prism-cli": "^5.14.2",
    "@types/node": "^24.2.1",
    "cross-env": "^10.0.0",
    "wait-on": "^8.0.4"
  },
  "dependencies": {
    "express": "^5.1.0"
  }
}
